<div class="flex ml-2">
  <% if next_label(card.status) %>
    <div>
      <%= button_to(
            next_label(card.status),
            card_path(id: card.id),
            method: :post,
            class: "#{new_button_class} advance"
          ) %>
    </div>
  <% end %>
</div>
